ServerAliveInterval 10
#ServerAliveCount 3
ServerAliveCountMax 3
ConnectTimeout 5

# sessions are stored at ControlPath and any attempt to connect to the same host becomes faster
ControlMaster auto
ControlPath ~/.ssh/%r@%h:%p
ControlPersist 4h
